# 🗄️ Database Migration Guide

คู่มือการจัดการโครงสร้างฐานข้อมูล (Schema) สำหรับโปรเจกต์ Dukdik Bot โดยใช้ **TypeORM** และ **PostgreSQL (Neon)**

## 🚀 คำสั่งที่เกี่ยวข้อง (Scripts)

คุณสามารถรันคำสั่งเหล่านี้ผ่าน `npm` ได้จากรูทของโปรเจกต์:

| คำสั่ง | คำอธิบาย |
| :--- | :--- |
| `npm run migration:generate` | ตรวจสอบความเปลี่ยนแปลงใน Entity และสร้างไฟล์ Migration ใหม่โดยอัตโนมัติ |
| `npm run migration:run` | นำไฟล์ Migration ที่ยังไม่ได้รัน ไปรันใน Database (สร้าง/อัปเดต Table) |
| `npm run migration:revert` | ยกเลิก (Undo) Migration ล่าสุดที่รันไป |

---

## 🛠️ ขั้นตอนการใช้งานทั่วไป

### 1. เมื่อมีการแก้ไข Entity
หากคุณมีการแก้ไขไฟล์ใน `src/database/entities/*.entity.ts` (เช่น เพิ่ม Column หรือเปลี่ยน Data Type):
```bash
npm run migration:generate
```
*ระบบจะสร้างไฟล์ใหม่ใน `src/database/migrations/xxxxxx-InitialMigration.ts`*

### 2. นำความเปลี่ยนแปลงไปใช้กับ Database
หลังจาก Generate ไฟล์แล้ว หรือเมื่อคุณ Pull Code มาจากเครื่องอื่น:
```bash
npm run migration:run
```

### 3. กรณีต้องการย้อนกลับ
หากรัน Migration ไปแล้วพบว่ามีข้อผิดพลาด:
```bash
npm run migration:revert
```

---

## ⚙️ การตั้งค่าระบบ (Technical Details)

- **Configuration**: การตั้งค่า Migration อยู่ในไฟล์ `src/database/typeorm-static-config.ts` ซึ่งจะดึงค่า `DATABASE_URL` จากไฟล์ `.env` โดยตรง
- **SSL**: ระบบเปิดใช้งาน `rejectUnauthorized: false` ไว้เพื่อให้รองรับการเชื่อมต่อกับ **Neon Postgres** ได้ทันที
- **Synchronize**: ในไฟล์ `src/app.module.ts` มีการตั้งค่า `synchronize: true` ไว้สำหรับการพัฒนาเบื้องต้น (ในระบบ Production แนะนำให้เปลี่ยนเป็น `false` และใช้ Migration แทน)

---

## 📢 ข้อควรระวัง
- **อย่าลบโฟลเดอร์ `migrations`**: ไฟล์ในนี้คือประวัติความเปลี่ยนแปลงของฐานข้อมูล
- **.env**: ตรวจสอบให้มั่นใจว่า `DATABASE_URL` ถูกต้องก่อนรันคำสั่ง Migration
- **Neon Postgres**: หากขึ้นข้อความ "SSL Error" ให้ตรวจสอบว่ามีการเติม `?sslmode=require` ใน Connection String หรือยัง
